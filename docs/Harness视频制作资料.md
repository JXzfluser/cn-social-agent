> 实现规格：[`docs/superpowers/specs/2026-08-11-web-presentation-douyin-design.md`](superpowers/specs/2026-08-11-web-presentation-douyin-design.md)  
> 实现计划：[`docs/superpowers/plans/2026-08-11-web-presentation-douyin.md`](superpowers/plans/2026-08-11-web-presentation-douyin.md)

# Harness 实践：让 Agent 全自动制作知识讲解视频

> 视频来源：https://www.youtube.com/watch?v=0hM2SkHZ2UU
> 频道：code秘密花园
> 时长：23:21 | 观看：5.9K | 点赞：376

## 一、视频核心目标

解答两个关键问题：
1. **Harness 有什么具体的工程实践？**
2. **视频效果到底是怎么做的？**

---

## 二、相关资源链接

| 资源 | 链接 |
|------|------|
| Skill 开源地址 | https://github.com/ConardLi/garden-skills/ |
| Claude Code 文档 | https://code.claude.com/docs/zh-CN/overview |
| MiniMax CLI | https://github.com/MiniMax-AI/cli |
| CC Switch | https://github.com/farion1231/cc-switch/releases/tag/v3.14.1 |

---

## 三、Harness 六大核心组件

一个成熟的 Harness，通常至少包含下面六个核心部分：

| 组件 | 它解决的问题 | 典型作用 |
|------|-------------|---------|
| **上下文管理** | 模型到底看到了什么 | 组织系统提示词、项目文档、历史对话、任务状态、外部资料 |
| **工具系统** | 模型到底能做什么 | 搜索、读写文件、调用 API、执行代码、操作浏览器 |
| **执行编排** | 模型下一步该做什么 | 决策、分步执行、工具调用、结果回写 |
| **状态与记忆** | 系统如何跨步骤保持连续性 | 保存任务进度、中间结果、长期偏好 |
| **评估与观测** | 系统怎么知道自己做得对不对 | 结果验收、过程追踪、指标监控、质量评估、错误归因 |
| **约束与恢复** | 出错了怎么办，怎么避免跑偏 | 权限控制、格式约束、重试机制、回滚与校验 |

---

## 四、视频制作四阶段工作流

### 阶段划分

```
Phase 1 内容编写：文章 → 口播稿 + 开发大纲
    ↓
Checkpoint A1 用户确认：稿子/主题/素材/开发模式
    ↓
Phase 2 构建演示：Vite/React/TS 网页开发（逐章或并行）
    ↓
Checkpoint B 确认是否合成音频
    ↓
Phase 3 音频合成（可选）：MiniMax CLI 串行合成
    ↓
Phase 4 录屏：浏览器全屏 + 自动播放 → OBS 录制
```

### 各阶段详解

#### Phase 1：内容编写
- AI 同时产出两样东西：**口播稿**和**开发大纲**
- 口播决定信息的描述方式以及整体节奏
- 大纲决定每章做几步、每步屏幕上放什么信息

#### Checkpoint A1（硬检查点）
Agent 被强制停下来，用户一次性对齐五件事：
1. 稿子要不要改
2. 大纲要不要改
3. 用哪套主题
4. 真素材谁准备
5. 后续章节是逐章做还是并行做

#### Phase 2：构建演示
- 第一章必须经人验收，确认视觉气质和节奏都对了，才成为后续章节的基准
- 之后的章节可以顺序做，也可以开多个子进程并行写
- 每章写完都要逐项对照清单自检，不过关不准说"做完了"

#### Checkpoint B（硬检查点）
决定要不要合成音频：
- 要 → 走 Phase 3
- 不要 → 跳到 Phase 4 用人声录

#### Phase 3：音频合成
- 从每章的口播文本里提取合成清单
- 调 TTS 逐段生成音频文件

#### Phase 4：录屏
- 浏览器全屏打开演示页
- 开自动播放模式
- 音频和画面天然对齐
- 录完裁头尾

---

## 五、三种播放模式

| 模式 | 参数 | 说明 |
|------|------|------|
| **手动** | 无 | 无音频，鼠标点击推进。适合检查画面细节，或自己录制音频 |
| **音频** | `?audio=1` | 手动推进，每步自动播放对应音频。自由控制节奏 |
| **自动** | `?auto=1` | 按空格后按音频节奏自动播放到底。配合 OBS 录屏直接出片 |

**自动模式原理**：第一次按空格绕过浏览器的自动播放限制，之后页面按音频时长自动推进，无需人工干预。

---

## 六、十条视觉设计原则

1. **16:9 固定舞台**：1920×1080 + transform scale，按视口缩放，没有响应式
2. **全局 step 计数器**：章节是 step 的纯函数，无定时器
3. **每步独占整屏**：每个节拍独占整屏，不堆叠项目符号
4. **口播节拍 = step**：讲述节奏直接映射为视觉 step
5. **隐藏的边角控件**：进度控制悬浮才出现，录屏画面保持干净
6. **舞台无 chrome**：没有 header/footer/页码/品牌条
7. **内容驱动动画**：先找内在动作，找不到才入场动画兜底
8. **多点逐个揭示**：1 项 = 1 step，禁同步 stagger 上 N 项
9. **整片同一主题**：颜色/字体走 token，章节间不翻表面色
10. **双源原则**：script 定节拍，article 定画面密度

---

## 七、实战演示数据

以 Anthropic 的一篇文章为例：

| 指标 | 数值 |
|------|------|
| 输入 | 一篇博客文章 |
| 输出 | 13 个章节、100+ 细粒度讲解步骤 |
| 画布 | 固定 16:9，适配任意显示器 |
| 交互 | 底部隐藏进度条，悬浮时自由跳转 |
| 画面 | 无页眉、无页码、无品牌标识——录屏时就是一块干净画面 |

---

## 八、音频合成流程

```bash
cd presentation
npm run extract-narrations  # 扫所有 narrations.ts → audio-segments.json
npm run synthesize-audio    # 调 mmx-cli 串行合成，增量跳过已存在
```

**合成后自动检查**：
- 哪些段时长异常（太长 = 该 step 拆分；太短 = 文案太薄）
- 给用户最后一次校准节奏的机会

---

## 九、完整工具链协作示例

假设你要做一个技术讲解视频：

```
1. CC Switch 切换到 Claude Code + 官方 Claude API
2. Claude Code 加载 web-video-presentation Skill
3. 输入文章 → Agent 产出口播稿 + outline
4. 用户确认（Checkpoint A1）
5. Agent 逐章构建 Vite/React/TS 演示
6. 用户确认（Checkpoint B）→ 选择合成音频
7. MiniMax CLI 串行合成每段口播音频
8. 浏览器开 ?auto=1，OBS 录屏一镜到底
9. 所有资产（文章、口播稿、大纲、代码、音频）进版本控制
```

---

## 十、web-video-presentation Skill 核心信息

### 简介
Web video / presentation - 将脚本、文章、课程、产品演示转换为可点击驱动的 16:9 网页演示，可录屏成电影级视频。

### 技术栈
- Vite + React + TypeScript
- 固定 1920×1080 舞台
- 点击/键盘驱动 `(chapter, step)` 游标

### 安装方式
```bash
npx skills add ConardLi/garden-skills -s web-video-presentation
```

### 主题系统
支持多种视觉风格：
- `paper-press` - 纸张出版社风格
- `terminal-green` - 终端绿色风格
- 等等

---

## 十一、Copaw 智能体构建指南

### 构建思路

基于上述工作流，在 Copaw 中构建智能体时需要：

1. **定义 Agent 角色**
   - 你是一个全自动视频制作助手
   - 专长：将文章转换为知识讲解视频

2. **上下文管理**
   - 系统提示词：说明 Agent 身份和能力边界
   - 技能加载：web-video-presentation Skill

3. **工具系统**
   - 文件读写
   - 命令执行（npm, git, OBS）
   - 浏览器操作

4. **执行编排**
   - 四阶段工作流嵌入 Agent 决策逻辑
   - 两个硬检查点必须暂停等待用户确认

5. **状态管理**
   - 跟踪当前阶段 (Phase 1-4)
   - 跟踪当前章节和步骤
   - 保存中间产物路径

6. **评估观测**
   - 每个阶段有验收标准
   - 自检清单驱动完成判断

### 关键检查点设计

```
检查点 A1:
- [ ] 口播稿已生成并确认
- [ ] 开发大纲已生成并确认
- [ ] 主题已选择
- [ ] 素材来源已确认
- [ ] 开发模式已选择（串行/并行）

检查点 B:
- [ ] 网页效果已验收
- [ ] 是否合成音频已确认
```

---

## 十二、关键洞察

1. **Harness 不是工具，是纪律**：模型有能力，但你需要系统来驾驭。划定边界、管理状态、设立检查点。

2. **硬检查点是 Skill 契约的一部分**：Agent 不应该从原文一路闷头做到成品。主题选择会影响动效气质，outline 确认能避免章节节奏跑偏。

3. **以后换一个题材不是从零开始**：换一篇原始文章，沿着同一条流水线跑一遍。

4. **技术约束替代文字约束**：用 verify.py 自动检查格式，而不是在 Prompt 里写"请确保输出格式正确"。
