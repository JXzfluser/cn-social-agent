# Nexus 工作台 · 使用说明（三分钟上手）

## 0. 启动与进入

```bash
# 一条命令启动（docker 里的 InsForge/Postgres 需要已在跑）
/Users/zfl/projects/cn-social-agent/scripts/start_workbench.sh
```

然后浏览器打开：**http://127.0.0.1:18080/nexus/**

登录账号：`demo@local.test` / `demo123456`

> 服务需要常驻。关掉终端或重启后，重跑上面那条命令即可（数据不丢）。

---

## 1. 三种起手式（对话输入框在页面底部）

| 想做什么 | 输入 | 你会得到 |
|---|---|---|
| 随便聊 / 问问题 / 写文案 | 直接打字 | 对话回答 |
| **做竖屏口播视频** | `/视频 为什么复利思维重要` | 12 镜分镜 → 确认 → 1080×1920 成片 |
| **做横屏讲解课件** | `/讲解 MCP 协议入门` | 大纲+口播稿 → 确认 → 20+ 页 slides |
| 没灵感 | 左侧「更多 → 热点看板」 | 热点 →「💡 选题」→「🎬 转视频」 |

输入 `/` 会唤起快捷菜单（模板 + 两条产线）。

---

## 2. 你会被要求确认的地方（AI 干活，人把关）

- **口播**：AI 出分镜文案 → 你读一遍 → 点「✅ 确认，开始渲染」→ 等 1-2 分钟
- **讲解**：AI 出大纲和口播稿 → 点「✅ 确认，开始构建」→ 等 1-3 分钟

确认前都可以直接改文案；渲染中途也能取消。

---

## 3. 成品在哪

- **口播成片**：对话卡片里直接播放 +「⬇ 下载成片」；「更多 → 制片记录」可回看所有片子（重开、重渲、发布）
- **讲解 slides**：对话里内嵌预览，点「⧉ 全屏预览 / 录制」用 OBS 录屏即成课
- **导出落盘**：`data/videos/<项目id>/final.mp4`、`data/exports/`

---

## 4. 发布一条视频（三个按钮）

1. 成片卡片点「✍️ 生成发布文案」→ 得到 3 个标题 + 6 个话题标签 + 简介（点击即复制）
2. 点「🚀 用此文案发布到抖音」→ 有凭证自动发，无凭证给半自动包（文案 + 创作者中心链接）
3. 「制片记录」里对任意已完成片子也能直接发布

---

## 5. 资料库（让 AI 用你的资料说话）

左侧「更多 → 资料库」上传文本/代码/CSV（≤200KB）。之后对话里问相关问题，
**相关资料会自动注入上下文**——不用再粘贴。

例：上传「品牌语料.md」后问“用我们的品牌语气写一句介绍”，助手会直接引用。

---

## 6. 常用能力速查

| 能力 | 入口 |
|---|---|
| 会话重命名 | 左侧会话列表 hover → ✎ |
| 重新生成回复 | 最后一条回复下方 ↻ |
| 模型切换 | 底部 ⚙ 图标（默认显示当前生效模型） |
| 自定义模板 | 广场 → 模板 →「+ 新建」，之后 `/` 菜单调用 |
| IM 远程指挥 | 设置 → IM 区块复制 webhook + secret，手机发指令 |
| 任务历史 | 更多 → 任务历史（专家派单 → 自动执行 → 人工验收 → 导出） |

---

## 7. 常见问题

**服务打不开 / 502**：重跑 `scripts/start_workbench.sh`；若提示JWT/PGRST301，
执行 `docker restart pgrst`（JWT secret 漂移问题）。

**渲染失败**：口播需要本机 `ffmpeg` 与 `edge-tts`；讲线需要服务以干净环境启动
（启动脚本已处理 `env -i`，不要直接用 `npm` 手动跑）。

**成片没声音**：检查 edge-tts 是否安装（`.venv/bin/pip install edge-tts`）。
